name: basex
project_files:
  - docker-compose.basex.yaml
  - basex/data/.gitkeep
  - basex/webapp/.gitkeep
  - basex/repo/.gitkeep

post_install_commands:
  - |
    #ensure directories exist and have correct permissions
    mkdir -p .ddev/basex/{data,webapp,repo}
    chmod 777 .ddev/basex/{data,webapp,repo} 