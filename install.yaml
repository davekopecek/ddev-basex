name: basex
project_files:
  - docker-compose.basex.yaml

post_install_commands:
  - |
    # Create directories and placeholder files
    mkdir -p .ddev/basex/{data,webapp,repo}
    touch .ddev/basex/{data,webapp,repo}/.gitkeep
    chmod 777 .ddev/basex/{data,webapp,repo} 